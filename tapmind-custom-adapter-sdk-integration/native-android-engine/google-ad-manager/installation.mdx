---
title: Installation
---

<Info>
**App Prerequisite**

* minSdkVersion of 23 or higher
* compileSdkVersion of 36 or higher
</Info>

### Configuration Steps

Ensure your `android/build.gradle` (project-level) includes the required repositories:

```
allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }  
    }
}
```

Open the `build.gradle` (Module: app) file.

Find the dependencies block and add from below for respective mediation platform.

```
dependencies {
    // Add TapMind SDK GAM Adapter dependency
implementation("io.github.tapmind-tech:customadapter-gam:2.1.11")
    // Add GAM SDK dependency
    implementation ("com.google.android.gms:play-services-ads:25.0.0")
}
```

